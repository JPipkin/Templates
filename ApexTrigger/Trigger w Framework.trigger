/*
** Trigger: {{ api_name }}
** SObject: {{ object_name }}
** Created by OpFocus on {{ today }}
** Description: 
**		+ 
*/

trigger {{ api_name }} on {{ object_name }} (before insert, before update, before delete, after insert, after update, after delete){
	//new {{ obj_abbrev }}TriggerHandler().run();
}